"""Authentication & authorization layer.

password.py — bcrypt-based hashing and verification.
jwt.py — encode/decode of stateless access tokens.
dependencies.py — FastAPI dependency that turns a token into a User.
"""
