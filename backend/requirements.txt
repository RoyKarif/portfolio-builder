# Pinned versions for reproducibility. Last verified: 2026-04-28.
# To upgrade: bump a line, rebuild the image, run tests.

# --- Web framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6        # ASGI server for FastAPI

# --- Database ---
sqlalchemy==2.0.35               # ORM
psycopg2-binary==2.9.9           # Postgres driver
alembic==1.13.3                  # Schema migrations

# --- Validation / settings ---
pydantic[email]==2.9.2           # Used by FastAPI internally too. [email] adds email-validator (required by EmailStr in schemas/auth.py).
pydantic-settings==2.5.2         # BaseSettings for env-var loading

# --- Auth ---
passlib[bcrypt]==1.7.4           # Password hashing (bcrypt backend)
bcrypt==4.0.1                    # Pinned: passlib has known issues with bcrypt>=4.1
python-jose[cryptography]==3.3.0 # JWT encode/decode
python-multipart==0.0.12         # Required by FastAPI for OAuth2PasswordRequestForm

# --- Math / data ---
numpy==1.26.4                    # Arrays, linear algebra
pandas==2.2.3                    # DataFrames for price series
cvxpy==1.5.3                     # Convex optimization (MVO solver)
scipy==1.13.1                    # Required by cvxpy; pin for reproducibility

# --- External data ---
# yfinance: scrapes Yahoo Finance. Yahoo periodically breaks the API,
# so we keep this on the latest 0.2.x and pair it with curl_cffi which
# impersonates a real browser (helps bypass anti-bot blocks).
yfinance==0.2.51
curl_cffi==0.7.4                 # Browser-fingerprint HTTP client used by yfinance
requests==2.32.3                 # yfinance fallback HTTP client

# --- Testing ---
pytest==8.3.3
pytest-asyncio==0.24.0           # For async test support
httpx==0.27.2                    # Required by FastAPI's TestClient
